# WeChatExportPlus v3.0.0

在 v2.2.0「批量导出」的基础上，把**"喂给 AI"这一步也搬进了软件里**：
给会话打标签、导出图片带会话名前缀、**内嵌 DeepSeek 官网页签**、导出内容**按批自动投喂**。

## 新增

### 1. 会话标签：勾一批 → 打包成标签 → 下次一键勾上
- 勾选若干会话 → 点 **「🏷 打包成标签」** → 选已有标签，或输入新名字新建
- 标签行上点一下 `#标签名`，就把带这个标签的会话**全部勾上**（替换当前勾选）
- 每行标题后面会显示它身上的标签
- 标签只存在你自己电脑上的 `会话标签.json` 里

### 2. 导出的图片文件名带会话名前缀
- 以前是 `0001.jpg`，AI 看不出这张图属于哪个群；现在是 **`群聊A_0001.jpg`**（仍在 `群聊A_图片\` 里）
- 正文里仍是 `[图片0001]`，和文件名末尾编号一一对应
- 「给 AI 的指令」、导出清单、头部说明的文案同步更新（不再让 AI 去找不存在的文件名）

### 3. 内嵌 DeepSeek 官网页签（真嵌入）
- 页头点 **「💬 DeepSeek 官网」**：官网**直接嵌在软件窗口里**（不是另开窗口），跟着主窗口缩放/最小化
- 软件切深色/浅色，官网配色跟着切
- 登录一次即可，登录态存在本机 `ds_profile\`
- 用的是软件自带的那份 Electron，**不额外增加体积**

### 4. 导出内容按批自动投喂（新增「发送清单」）
- 在官网页签的左栏勾选要发的内容 → 点 **「🚀 开始发送」**，之后全自动：
  挂附件 → 等文件处理完 → 点发送 → 等它答完 → 发下一批
- 默认 **只发文档（建议）**，需要图片时把勾去掉
- **最后一批会自动附带一句「我已发送完毕」**，触发 AI 开始正式分析
- 进度窗显示批次进度和**剩余时间**；万一某批没发出去，会明确告诉你该怎么手动补

### 5. "给 AI 的指令"升级成分批接收协议
- 发送时**第一个发** `给AI的指令.txt`，里面写明：在所有文件发完之前，每次只回一句
  「我已接收上述信息」，不要展开分析；直到收到「我已发送完毕」再开始正式分析
- 这样每批回复都极短，**不吃上下文**，也不需要靠"截断"来省 token
- **发送前会自动把旧导出目录里的指令文件刷新成最新版**（不会再用老文案）

### 6. 微信数据目录找得更准
- 优先读**微信自己记录的位置**：微信 4.x 的 `%APPDATA%\Tencent\xwechat\config\`、
  微信 3.x 的注册表 `FileSavePath` —— 你把数据挪到自定义目录也能自动找到
- 同时支持「文档」被 OneDrive 重定向、以及盘符下一层的自定义目录
- 实在找不到时，卡片仍会提示「未检测到 / 请点『浏览』选择」手动兜底

## 界面

- 会话页新增标签行和「打包成标签」入口
- 官网页签左下角：**「只发文档（建议）」** + **「每批 N 个 ⇄」**（点一下切换 10/20/30）
- 官网页签顶部有醒目提示，提醒附件数量别过量

## 一个实测结论（重要）

官网文档写"单次最多 50 个文件"，但实测**一批 40 个附件就会被拒收**
（每个附件显示「服务器繁忙」、整条消息报「请删除异常文件再发送」），**30 个以内才稳定**。
所以 3.0 的默认是：**每批 30 个**、**只发文档**、每个文件挂上后**等一会儿再点发送**
（文档 0.25 秒/个、图片 0.15 秒/个）。

## 下载

下载下面的 `WeChatExportPlus_v3.0.0_win64.zip`，解压后双击 `WeChatExportPlus.exe`（或 `启动工具.bat`）。

- 系统：Windows 10/11 64 位；微信需处于**已登录**状态
- 首次运行/获取密钥会申请管理员权限（读取微信数据库需要）
- 从 2.2.0 升级：直接覆盖即可；想保留设置/标签/登录态，把旧目录的 `.ui_settings`、
  `会话标签.json`、`ds_profile\` 复制过去

## 已知限制

- 只支持 Windows + 微信 4.x 数据目录；只能抓**这台电脑上存着的**消息
- 网页版不认文件夹；图片受上面那个数量上限约束（超量会被官网拒收）
- 内嵌官网是"移除官网嵌入限制"的做法，**官网改版可能失效**；自动发送属于自动化操作，
  建议先小批量试，并自行评估账号风险
- 打包成单文件 exe，**杀毒软件可能误报**

## 隐私

本工具**完全本地运行**，程序本身不联网。只有你主动打开「DeepSeek 官网」页签、
并且点了「开始发送」时，内容才会发到 DeepSeek。发布包里**不含**任何用户设置、标签或登录态。

## 许可

内核基于 [Ray0612/WeChat-Export-Tool](https://github.com/Ray0612/WeChat-Export-Tool)（GPL-3.0），
本项目同样以 GPL-3.0 发布。详见仓库内 LICENSE 与说明文档。

## 反馈

有问题请开 [Issue](https://github.com/Lan-DQ/WeChatExportPlus/issues)，附上操作步骤和截图。
