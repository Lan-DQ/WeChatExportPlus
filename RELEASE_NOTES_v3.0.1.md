# WeChatExportPlus v3.0.1（小补丁）

修两个实际用起来才会碰到的问题，功能不变。

## 修复

### 1. 切到别的页面再切回来，官网页签打不了字
内嵌的官网页是"跨进程子窗口"（Electron 的窗口被挂进软件窗口里）。Windows 只会把键盘
消息派发给**前台窗口所在输入队列**里的焦点窗口，跨进程的子窗口不在那条队列上 ——
所以点进输入框看着有焦点、打字却毫无反应。现在切回官网页签时会：
- 把软件线程和浏览器线程的输入队列接起来（`AttachThreadInput`），让键盘消息真正送进页面；
- 同时请 Electron 把渲染层的输入焦点也恢复回来。

### 2. 多了一条"发给 AI"的输入框（官网页签顶部）
万一内嵌页还是收不到键盘（官网改版、输入法异常等），可以直接在软件里输入：
在官网页签顶部的输入框里写内容 → 点「发送」（或按回车），文字会被直接写进官网的输入框并发送。
这条路走 Electron 的 `insertText`，**不依赖操作系统键盘焦点**，一定可用 ——
比如发那句「我已发送完毕」。

顺带修掉一个相关问题：**纯文字消息**（没有附件那种）发出去之后，输入框会立刻变成只读，
原来的"确认已发送"判断拿不到证据，会误报"没发成功"；现在补了一条判据（发送前没在生成、
发送后开始生成了即视为提交成功）。

## 给 AI 的指令：把"怎么要图片"写清楚了

以前只写了"需要图片时告诉我编号"，实际用起来 AI 会说"第 7 张图""你发的那张截图"，
而你根本不知道要上传哪一个。现在指令里明确要求按 **「会话名 + 图片序号」** 的格式逐条列出，例如：

```
需要：群聊A_图片\群聊A_0007.jpg（对应正文 [图片0007]）
需要：群聊B_图片\群聊B_0012.jpg（对应正文 [图片0012]）
```

并禁止"第 7 张图""某张截图"这类没法定位的说法。
（导出目录里的 `给AI的指令.txt` 在**每次点「开始发送」时都会自动刷新成最新版**，
所以旧导出目录也不用重新导出。）

## 下载

下载下面的 `WeChatExportPlus_v3.0.1_win64.zip`，解压后双击 `WeChatExportPlus.exe`。
从 3.0.0 升级直接覆盖即可（想保留设置/标签/登录态，把旧目录的 `.ui_settings`、
`会话标签.json`、`ds_profile\` 复制过去）。
