# WeChatExportPlus v3.2.0

<!--
  这是发布说明的**骨架**：`【】` 里是要你自己填的内容。
  结构照抄了往期 RELEASE_NOTES_v3.0.1.md：一句话开头 → 修复 → 下载。
-->

【一句话：这版主要解决了什么 / 为什么值得升级】

## 修复

### 1. 同一台电脑上有多个微信账号时，连数据库一直超时
【填：这个问题的现象（issue #2 那位：获取到密钥但连不上、一直超时）】
【填：原因（程序找 session.db 是"递归找到第一个就用"，不校验密钥属于哪个账号，
      旧账号的记录还在时会拿错库）】
【填：现在怎么用（首页多了「微信账号」一张卡：检测到多个账号时点「切换」选对的那个；
      只有一个账号会自动选中）】

### 2. 图片解密太慢
【填：现象（每张图起一个 node 进程，400 张要十几分钟）】
【填：现在（一个进程批量处理，实测 69ms/张 → 2ms/张）】

### 3. 退出软件后有残留进程
【填：现象】
【填：现在（按进程树杀 + 校验残留；关窗后零残留）】

### 4. 官网页签：内嵌窗口的显示问题
【填：可选。这轮改了拖动跟随、提示/弹窗让位、发送时窗口空白等】
【如果不想展开，整条删掉即可】

## 下载

下载下面的 `WeChatExportPlus_v3.2.0_win64.zip`，解压后双击 `WeChatExportPlus.exe`。

【填：升级说明，例如"从旧版升级直接覆盖即可；想保留设置/标签，把旧目录的
      `.ui_settings`、`会话标签.json` 复制过去"】

---

## 附：可选的"已知问题 / 说明"

【填：如果这版还有已知没解决的，写在这里，例如"必须用微信 4.x；
      3.x 的数据结构不同，暂不支持"】
