# WeChatExportPlus v2.2.0

界面重写版：**批量勾选会话 → 一键导出成可直接喂给 AI 的语料**。

## 这个版本修了什么

上一版（v2.1.0）的界面存在一批**影响可用性**的问题，这一版全部修复：

| 问题 | 原因 |
|---|---|
| **所有按钮都点不动** | tkinter 的 `event.type` 是 `EventType` 枚举而不是 `int`，查表永远查不到，事件被静默丢弃 |
| **背景一直是白底** | 背景图只生成了、没画到画布上（正常启动路径从未执行到绘制） |
| 窗口一闪就多一个控件 | 重绘时旧行图元没清理，反复重绘会不断叠加 |
| 勾选状态显示错乱 | 复选框图用了单槽缓存，整个列表的勾选外观被最后画的那行污染 |
| 只能拖滚动条，滚轮没用 | `MouseWheel` 没登记进事件映射表 |
| 关窗口卡一下 | 数据库清理阻塞在关窗之前（现在先关窗，清理转后台） |
| 某一行行为诡异 | 页面重建后旧控件仍留在输入名单里，用旧状态操作新画布 |

## 新增

- **目录记忆**：导出目录、工作目录选一次就记住，下次开程序不用重选
  （目录若已不存在会自动回退到桌面）
- **滚轮/中键滚动**，快速滚动按实际 delta 倍数翻页
- **新图标**（用户提供立绘裁剪，含 16~256px 多尺寸）
- 关窗即时响应（原来最坏要等十几秒）

## 界面

- 渐变背景 + 柔和光晕，支持放自己的图（程序目录放 `背景.png` 即生效）
- 玻璃拟态面板，圆角走 PIL 超采样抗锯齿
- 深浅主题可切换，选择会记住

## 会话列表操作

- 点最左边的方框 → 勾选 / 取消
- 点该行其它位置 → 打开该会话的详细内容
- 顶部：全选 / 全不选 / 反选 / 全选搜索结果
- 滚轮或拖动滚动条翻页

## 导出

导出的每个会话都是一个 `.md` 单文件（默认格式），**直接拖进 DeepSeek 网页版**即可 ——
不需要文件夹支持。图片以相对路径编号引用，并附带「给 AI 的指令」说明 AI 看不到图片内容。

勾选「导出并清除之前的导出内容」可每次覆盖，它只删除程序自己生成的东西
（`导出_日期_时间` 目录、`导出清单.html`、`给AI的指令.txt`），你放在同目录的其它文件不会被碰。

## 下载

下载下面的 `WeChatExportPlus_v2.2.0_win64.zip`，解压后双击 `WeChatExportPlus.exe`。

- 系统：Windows 10/11 64 位
- 首次运行会申请管理员权限（读取微信数据库需要）
- 微信需处于**已登录**状态

## 已知限制

- 只支持 Windows，且只支持微信 4.x 的数据目录
- 图片按会话归档到 `会话名_图片\` 子目录，需要手动一起拖给 AI
- 单会话过大时 `.md` 会很长，建议按需分批导出

## 隐私

本工具**完全本地运行**，不会上传任何聊天记录。数据库密钥只保存在你自己的磁盘上。

## 许可

内核基于 [Ray0612/WeChat-Export-Tool](https://github.com/Ray0612/WeChat-Export-Tool)（GPL-3.0），
本项目同样以 GPL-3.0 发布。详见仓库内 LICENSE 与说明文档。

## 反馈

有问题请开 [Issue](https://github.com/Lan-DQ/WeChatExportPlus/issues)，附上操作步骤和截图。
