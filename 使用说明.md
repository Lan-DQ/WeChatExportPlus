# 微信聊天记录批量导出工具

> v2.0.0 · 基于 [Ray0612/WeChat-Export-Tool](https://github.com/Ray0612/WeChat-Export-Tool) v1.2.0 内核
> 主要用途：把多个微信群 / 私聊的聊天记录批量导出成**适合喂给 AI 助理**的语料

---

## 1. 快速开始

双击 `WeChatExportPlus.exe`，或 `启动工具.bat`。整个文件夹可以拷到任意位置（U 盘、别的电脑）。

1. **微信数据目录** —— 一般会自动检测到，不用改
2. **导出工作目录** —— 日志与密钥存放处，默认桌面 `wx_export`
3. **数据库密钥** —— 如果之前用过原版工具，密钥会自动加载；否则点「🔑 获取密钥」（会要求你先关掉微信）
4. **点「🗄️ 连接数据库」** —— 成功后会提示有多少个会话
5. **点「📤 浏览会话」** —— 进入会话列表

### 会话列表怎么用

- **点最左边的方框** → 勾选 / 取消勾选
- **点这一行的其它任何位置** → 打开该会话的详细内容
- **全选 / 全不选 / 反选 / 全选搜索结果** —— 顶部四个按钮
- **搜索** 只影响显示，不影响已勾选的内容（可以先搜"群"→全选搜索结果，再搜别的）
- **滚轮**翻页；也可以**直接拖动右侧滚动条**滑块，或点轨道空白跳到该位置
- 群聊前面有 👥 标记
- 鼠标移到某一行会高亮，方便看清点的是哪一行

### 导出

底部「统一导出设置」里选**一种格式**（一次导出所有勾选的会话都用这个格式），再选导出位置，点「📦 开始导出」。

**默认格式是「Markdown 单文件」**——它就是为了"直接拖进 DeepSeek 网页版"设计的，
不用改任何设置，导完打开文件夹把 `.md` 拖走即可（详见第 3 节）。

**勾选「导出并清除之前的导出内容」**可以每次覆盖上一次的结果，省磁盘空间。
它只删除**程序自己生成的**东西（`导出_日期_时间` 目录、`导出清单.html`、`给AI的指令.txt`），
你放在同一目录下的其它文件**不会被碰**。

每次导出都会自动带上**「给 AI 的指令」**（告诉 AI 它看不到图片内容、该怎么应答等），
它作为**一个独立的 `给AI的指令.txt`** 放在导出目录里（不会重复写进每个 `.md`）。
你可以点「✏️ 编辑给AI的指令」随时改（第 3.5 节）。

导出过程中会显示「第 N/M 个：会话名」，可以随时**取消**（会在当前会话处理完后停下，不留半个文件）。

### 界面外观

- 右上角按钮可以**切换深色 / 浅色主题**，选择会被记住
- 想换背景图：把图片命名成 `背景.png`（或 `background.png`）放到程序目录，重启即可。
  什么都不做也行，程序自带渐变背景。

---

## 2. 导出结果长什么样

假设你勾选了「群聊A」「私聊B」，导出到桌面，用默认的 `Markdown 单文件` 格式：

```
桌面\
└── 导出_20260213_1430\          ← 整个文件夹是一次导出的归档
    ├── 群聊A.md                ← ★ 目标文件：直接在这一层，双击可读，可直接拖进网页版
    ├── 群聊A_图片\             ← ★ 这个会话的图片，按 0001.jpg 顺序编号
    │   ├── 0001.jpg
    │   └── ...
    ├── 私聊B.md                ← ★ 另一个会话的目标文件，同样在这一层
    ├── 私聊B_图片\
    ├── 给AI的指令.txt           ← 本次使用的指令原文（整个导出只此一份）
    └── 导出清单.html            ← 总索引，列出本次所有会话，可点击跳转
```

**关键：`.md` 文件直接在导出文件夹的最外层**，没有被套进「会话名」子文件夹里，
所以你打开这个文件夹就能直接把它拖走。

**「给 AI 的指令」每个导出目录只放一份**（`给AI的指令.txt`），不会重复塞进每个 `.md` 里 ——
会话多的时候那样纯属浪费空间。拖给 AI 时把这个 txt 和 `.md` 一起给它即可。

其它格式的落盘方式：

| 格式 | 落盘方式 |
|---|---|
| **Markdown 单文件**（默认） | 根目录下 `<会话名>.md` + `<会话名>_图片\` ← 就是上面这种 |
| AI 语料 JSONL | 每个会话一个文件夹，内含 `对话.jsonl` + `对话.txt` + `图片\` |
| HTML | 每个会话一个文件夹，内含 `index.html` + `图片\`（气泡页面，支持搜索） |
| Excel | 每个会话一个文件夹，内含 `<会话名>.xlsx` |
| CSV | 每个会话一个文件夹，内含 `对话.csv` |
| PDF | 每个会话一个文件夹，内含 `<会话名>.pdf`（图片已内嵌） |
| TXT | 每个会话一个文件夹，内含 `对话.txt` |
| JSON | 每个会话一个文件夹，内含 `对话.json` |

> 同一分钟内重复导出不会互相覆盖，会生成 `导出_20260213_1430_2`。
> 同名会话（不同 wxid 但显示名一样）也不会互相覆盖，第二个会叫 `群聊A_2`。

---

## 3. 怎么喂给 DeepSeek 网页版（主要用法）

### 3.1 三步走

1. 导出格式选默认的 **Markdown 单文件**，导出。
2. 打开导出文件夹，把 `群聊A.md` **拖进 DeepSeek 对话框**（或双击打开后全选复制粘贴）。
3. 如果这一轮需要看图，再把 `群聊A_图片\` 里的图**按编号拖进去**，
   正文里对应写着 `[图片0001]`，编号一一对应。

`.md` 是纯文本格式，网页版当作长文读取，不需要联网渲染，86 万字符级别的会话也没有问题。

### 3.2 为什么不让图片自动嵌在 .md 里

我试过的两条路都不通，所以最终选了「正文标编号 + 图片放旁边文件夹」：

| 设想 | 实际问题 |
|---|---|
| `.md` 里写 `![](图片/xxx.jpg)` | 网页版拿到的是**上传的文件本身**，看不到你磁盘上的相对路径，图片不会显示 |
| `.md` 里直接内嵌 base64 图片 | 网页版不渲染 data URI，等于把一大段乱码塞给模型，**白烧 token 还干扰理解** |

所以最可靠的是：**文本用 `.md` 全量喂，图片按需单独拖**。

### 3.3 一个会话一个文件，多个会话怎么办

网页版**每次最多上传 50 个文件、单文件最大 100MB**。
如果你勾了多个会话，就会得到多个 `.md`；按需要挨个拖即可（一个群一天的记录就够大了，
不建议一次全塞——上下文会被填满，模型反而抓不住重点）。

### 3.4 `.md` 长什么样

```markdown
# 群聊A

- 会话类型：群聊
- 参与者（51 人）：赵六、user_x、钱七、王五、…… 等 51 人
- 消息数：930 条
- 时间范围：2026-02-13 ~ 2026-02-13
- 图片：329 张，正文里写 `[图片NNNN]`，对应同目录下 `群聊A_图片\NNNN.jpg`

> 每条消息格式为 `[时间] 发言人: 内容`。`(系统)` 表示系统消息，`我` 表示本人。

---

[2026-02-13 09:14:03] (系统) "某同学"通过扫描"user_c"分享的二维码加入群聊
[2026-02-13 11:10:35] 王五: 某条消息
[2026-02-13 12:29:53] 赵六: [图片0001]
[2026-02-13 18:04:28] 真: [位置] 某地标
[2026-02-13 18:22:10] user_c: [群公告] 某通知：
    1、楼内禁止吸烟。
    2、寝室内严禁使用违规电器（吹风机、美发棒、制冷机，温热杯等小家电）。
```

注意几点设计：

- **头部有参与者名单、消息数、时间范围** —— 模型一上来就知道这是什么群、有哪些人。
- **每条消息一行，第一列固定是 `[时间] 发言人:`** —— 谁是说话人一目了然。
- **多行消息的续行缩进 4 空格** —— 不会和下一行的 `[时间]` 混淆。
- **图片写成 `[图片0001]`** —— 4 位定宽编号，和 `_图片\0001.jpg` 严格对应，超过 99 张也不会错位。

---

## 3.5 「给 AI 的指令」——每一次导出都会自动带上

DeepSeek 网页版**没有"系统提示词"这个概念**，你没法预设"你要当我的助理"。
所以程序会把这段要求放进导出结果里，AI 一读到就知道该怎么做。

### Markdown 格式（默认）：指令是独立的一个 txt

**`.md` 正文里不再夹带指令** —— 会话多的时候，每个文件都塞一份纯属浪费
（正文可能才几 KB，指令本身就占 1 KB）。

指令作为**一个独立的 `给AI的指令.txt`** 放在导出目录里，整个导出只有这一份。
拖给 AI 时把它和 `.md` 一起给即可。

> 如果只需要其中一两个会话，也可以把那个 txt 和新对话一起贴给 AI，效果一样。

### 其它格式的指令位置

| 格式 | 指令写在哪 |
|---|---|
| TXT | 文件开头，用 `#` 注释包起来 |
| JSONL | `_meta.ai_instruction` 字段 |
| JSON | `_ai_instruction` 字段 |
| HTML | 页面顶部一个可折叠的黄色提示框 |
| PDF | 单独一页放在正文之前 |
| CSV | 追加在文件末尾（用标记行隔开） |
| Excel | 最前面几行（红色小字），表头和数据整体下移 |

另外导出目录下始终会放一份 `给AI的指令.txt`，方便你复制去改。

### 怎么改（不用重新打包程序）

1. 在会话列表底部点 **「✏️ 编辑给AI的指令」** → 会用记事本打开程序目录下的 `AI提示词.txt`
2. 改完保存关闭
3. 回到程序点 **「重载指令」** 让它立即生效（不点也行，下次导出会重新读）

也可以直接去程序目录用任何编辑器打开 `AI提示词.txt`。

### 默认指令写了什么

默认版本是你原来那份要求的细化版，主要包含：

- **角色**：私人助理兼顾问
- **最重要的一条**：明确告诉 AI「你看不到图片内容」，`[图片0001]` 只是占位，
  **禁止猜测图片内容**；需要看图时应当向你索要编号
- **其他拿不到内容的类型**：语音/视频只有时长、链接小程序只有标题、文件只有文件名、
  表情只有占位 —— 要求 AI 说明"这里内容看不到"并指出位置，不要跳过不提
- **分析纪律**：只依据文件里实际存在的文字作答；注意时间顺序、核对信息是否变更、
  关键信息给出处（时间 + 发言人）
- **回答风格**：先简短结论，再按需细化
- **优先级**：你的当次指令永远高于这些默认设定

改这个文件就等于改 AI 的行为，不用动程序。

---

## 4. 给 AI 用：另一种格式（JSONL，做知识库时用）

如果你不是"整篇丢给模型"，而是想把聊天记录**入库、检索、做 RAG**，那用 **AI 语料 JSONL** 格式：
每个会话一个文件夹，里面有 `对话.jsonl` 和 `对话.txt`。

### 4.1 `对话.jsonl`

第一行是元信息，之后每行一条消息：

```json
{"_meta": {"chat": "群聊A", "chat_id": "12345678@chatroom", "is_group": true,
           "message_count": 920, "date_from": "2026-02-13", "date_to": "2026-02-13"}}
{"id": "12345678@chatroom:1787454635:36", "ts": 1787454635,
 "time": "2026-02-13 11:10:35", "sender": "王五",
 "sender_id": "wxid_2jifohn2zq2v22", "role": "other", "type": "text",
 "text": "某条消息", "chat": "群聊A", "chat_id": "12345678@chatroom",
 "is_group": true, "render": "[2026-02-13 11:10:35] 王五: 某条消息"}
```

字段含义：

| 字段 | 说明 |
|---|---|
| `time` / `ts` | 可读时间 / Unix 时间戳 |
| `sender` | **发言人显示名**（自己显示为「我」） |
| `sender_id` | 发言人的原始 wxid（显示名会变，wxid 不会） |
| `role` | `self`=本人、`other`=对方、`system`=系统消息 |
| `type` | 消息类型：`text` `image` `voice` `video` `file` `link` `quote` `chatrecord` `location` `card` `call` `sticker` `system` `transfer` `redpacket` `solitaire` `groupannounce` `miniprogram` `app` |
| `text` | 该消息的完整可读文本 |
| `image` | 图片相对路径（仅 `type=image`），如 `图片/xxx.jpg` |
| `extra` | 结构化补充（链接的 url、文件的文件名与大小、位置坐标、引用原文、视频时长…） |
| `render` | **已经拼好的单行文本**，`[时间] 发言人: 内容`，不想自己拼就直接用它 |

### 4.2 `对话.txt`

```
# 聊天记录：群聊A
# 会话ID：12345678@chatroom
# 类型：群聊
# 消息数：920
# 时间范围：2026-02-13 ~ 2026-02-13

[2026-02-13 09:14:03] (系统) "某同学"通过扫描"user_c"分享的二维码加入群聊
[2026-02-13 11:08:16] 王五: [表情]
[2026-02-13 11:10:35] 王五: 某条消息
[2026-02-13 12:29:53] 赵六: [图片: 图片/a52328148bd6350e394d89f424907303.jpg]
[2026-02-13 18:04:28] 真: [位置] 某地标
```

第一列永远是 `[时间] 发言人:`，后面才是内容 —— 这样：
- 用固定长度切块做 RAG 时，每块都能看出是谁在说话；
- 单行消息可以用正则直接切：`^\[(.+?)\] ([^:]+): (.*)$`

> ⚠️ **但多行消息（群公告、接龙、转发的聊天记录）会占好几行**，后续行以 4 个空格缩进，
> 上面的正则会漏掉它们。实测某个群里 1210 行中有 827 行能被该正则匹配，其余是多行消息的续行。
> **要做严格解析请用 `对话.jsonl`**（每行一条，`text` 字段是完整内容，天然不会被换行破坏）；
> `对话.txt` 更适合整篇阅读或粗略切块。

### 4.3 把 JSONL 和图片一起发给 API

见第 5 节 —— 那是调用 **API** 的做法（网页版不需要，看第 3 节即可）。
API 支持把图片以 base64 塞进请求，所以可以「一次把文本和图片都发过去」。

---

## 5. 图片怎么喂给 API（可选，网页版不需要）

**导出的 `图片\xxx.jpg` 是磁盘上的普通文件，DeepSeek 看不懂「文件路径」。**
DeepSeek 的图像输入只支持三种方式（见[官方文档](https://api-docs.deepseek.com/zh-cn/guides/vision/)）：

| 方式 | 适用 |
|---|---|
| Base64 内联 `data:image/jpeg;base64,...` | 本地文件最简单 |
| 公开可访问的 `http(s)` 链接 | 模型自己下载（需公网、60 秒内可下载） |
| Files API 上传后引用 `file_id` | 多次复用同一张图；图片大或请求体超 48 MiB |

另外 DeepSeek 的 **Files API 只收图片**（JPEG/PNG/GIF/WebP），不能上传 txt/jsonl 当文本语料 —— 文本要直接放进 `messages`。

### 5.1 直接读 jsonl 并带上图片（可运行示例）

```python
import base64, json, os

def load_chat(chat_dir, max_images=None):
    """把一份导出读成 DeepSeek 能吃的 content 数组。"""
    content = []
    with open(os.path.join(chat_dir, '对话.jsonl'), encoding='utf-8') as f:
        meta = json.loads(f.readline())['_meta']
        content.append({'type': 'text', 'text':
            f"以下是群「{meta['chat']}」的聊天记录"
            f"（{meta['date_from']} ~ {meta['date_to']}，共 {meta['message_count']} 条）。"
            f"每行格式为 [时间] 发言人: 内容。"})

        n_img = 0
        for line in f:
            rec = json.loads(line)
            content.append({'type': 'text', 'text': rec['render']})

            # 图片消息：把图读成 base64 跟在文本后面
            if rec.get('image'):
                if max_images is not None and n_img >= max_images:
                    continue
                path = os.path.join(chat_dir, rec['image'].replace('/', os.sep))
                if not os.path.exists(path):
                    continue
                with open(path, 'rb') as im:
                    b64 = base64.b64encode(im.read()).decode()
                ext = os.path.splitext(path)[1].lstrip('.').lower()
                mime = {'jpg': 'jpeg', 'jpeg': 'jpeg', 'png': 'png',
                        'gif': 'gif', 'webp': 'webp'}.get(ext, 'jpeg')
                content.append({'type': 'text',
                                'text': f"[图片 posted by {rec['sender']} at {rec['time']}]"})
                content.append({'type': 'image_url',
                                'image_url': {'url': f'data:image/{mime};base64,{b64}',
                                              'detail': 'low'}})
                n_img += 1
        return content

# 用法
from openai import OpenAI
client = OpenAI(api_key='<你的key>', base_url='https://api.deepseek.com')
content = load_chat(r'C:\Users\你\Desktop\导出_20260213_1430\群聊A', max_images=20)
resp = client.chat.completions.create(
    model='deepseek-flash',
    messages=[{'role': 'user', 'content': content}])
print(resp.choices[0].message.content)
```

### 5.2 图片很多时的成本提醒

- 每张图最多算 **1024 tokens**（超过约 1300×1300 后不再增加）。
- 单次请求最多 **600 张**图；内联图片请求体上限 **48 MiB**（用 `file_id` 可到 200 MiB）。
- 所以「一个 900 条消息的群 + 325 张图」不要一次性全塞进去：
  建议 **文本全量 + 图片按需**（先让模型看文本，需要时再取某几条的图）。

### 5.3 建议的助理接法

1. **建索引**：把每个会话的 `对话.jsonl` 按行入库（SQLite / 向量库都行），`render` 直接当文本、`sender`/`time` 当元数据过滤。
2. **检索**：用户提问 → 检索相关消息 → 把命中消息的 `render` 拼进 prompt。
3. **带图**：命中 `type=image` 的记录时，用上面的 `data:` URL 方式把对应图片附上。
4. **全量长文**：如果只是要"读完整段对话"，用 `对话.txt` 整篇丢进去，比 HTML/PDF 省得多。

---

## 6. 已知限制（务必了解）

这些都是**数据本身的边界**，不是程序 bug：

| 项 | 情况 |
|---|---|
| **语音** | 只有时长（`[语音 4.6秒]`），没有转文字。要文字需另做语音识别，且需先下载语音文件（本工具不下载）。 |
| **视频** | 只有时长（`[视频 10秒]`），不下载视频文件。 |
| **表情/表情包** | 只有 `[表情]`。微信的 GIF 表情有单独的下载链路，本工具不做。 |
| **文件消息** | 有文件名和大小，但不下载文件本体。 |
| **小程序 / 视频号 / 公众号文章** | 有标题和链接（`extra.url`），但拿不到实际内容 —— 那需要登录态请求，本工具不做。 |
| **合并转发的聊天记录** | 已支持展开（你的库里目前没有这种消息，所以这条路径未经真实数据验证）。 |
| **图片** | 已解密导出为 jpg/png。但**图片里写的字 AI 看不到**，除非你按第 3 节的办法把图也拖进去。 |
| **被撤回的消息** | 只能看到「谁撤回了一条消息」，原文微信自己已经删了。 |
| **导出速度** | 图片解密每张约 0.1 秒。900 条消息 + 329 张图约 40 秒；全选 128 个会话建议分批。 |
| **PDF 里的 emoji** | 中文字体不含 emoji 字形，会显示成空白/方块。要 emoji 请用 Markdown / HTML / AI 格式。 |
| **HTML 体积** | 图片以文件形式引用，所以必须整个文件夹一起拷贝，不能只拷 `index.html`。 |
| **会话消息量很大** | 你最大的群有 4462 条消息，`.md` 约 250KB 文本。一次全塞给模型会占满上下文，建议按时间分段或先检索再问。 |

### 隐私提醒

导出的聊天记录包含你和他人之间的私密对话。**上传给云端 AI 等于把这些内容交给了模型厂商。**
如果介意，请改用本地模型（Ollama 等），导出的 `.md` / `对话.jsonl` / `对话.txt` 一样能用。

---

## 7. 和原版工具的关系

| | 原版 v1.2.0 | 本工具 v2.0.0 |
|---|---|---|
| 选会话 | 双击一个会话 → 单独窗口 | **勾选多个会话**，一次导出 |
| 选格式 | 每个会话单独选 | 在会话列表**统一设置一次** |
| 输出 | 全部文件平铺在工作目录 | **带时间戳的归档文件夹**；Markdown 格式的目标文件直接在该层 |
| 网页版可用 | 不能（输出是散文件/文件夹） | **`<会话名>.md` 单文件，可直接拖进 DeepSeek 网页版** |
| 索引 | 无 | **`导出清单.html`** |
| 进度 | 单个会话进度窗 | **第 N/M 个 + 可取消** |
| AI 语料 | 无 | **`对话.jsonl` + 带发言人的 `对话.txt`** |
| 消息类型 | 只有文字和图片，其余是 `[类型49]` | **语音/视频/链接/文件/引用/位置/名片/通话/转账/红包/群公告/系统消息全部可读** |
| 发言人 | 群聊基本分不清 | **每条都带发言人**（Markdown 是 `[时间] 发言人: 内容`，JSONL 另有 `sender_id`/`role`） |
| 图片 | 位置不定、命名是 md5 | **按出现顺序 `0001.jpg` 编号，和正文 `[图片0001]` 一一对应** |

内核（密钥提取、WCDB 数据库解密、图片解密）完全沿用原版，未做改动，
仅有一处必要修正：`wcdb_server.js` 里 ZSTD 解压的长度上限从 10000 提到 1000000
（原上限会让超长消息无法解压而被静默丢弃，实测你的库里有 2 条受影响）。

原版工具仍然保留在 `C:\My_GongJu\grab\grab\WeChatExport`，未被修改或覆盖。

---

## 8. 从源码构建

```bash
# 1. 安装依赖
python -m pip install --user pycryptodome fpdf2 openpyxl Pillow pyinstaller pytest

# 2. 开发模式运行（需要在项目根目录放好 runtime/ dll/ resources/，见下）
python build_dist_plus.py --skip-build    # 只组装运行时目录
python gui/app_plus.py

# 3. 跑测试
python -m pytest tests/ -v

# 4. 打包发布
python build_dist_plus.py
# 产物：dist/WeChatExportPlus/
```

`build_dist_plus.py` 需要原版 `WeChatExport` 目录提供运行时文件（node.exe / dll / resources）。
如果不在默认位置，用 `--kernel` 指定：

```bash
python build_dist_plus.py --kernel "D:\某处\WeChatExport"
```

---

## 9. 许可证

内核来自 [Ray0612/WeChat-Export-Tool](https://github.com/Ray0612/WeChat-Export-Tool)，GPL-3.0，
许可证原文见 `LICENSE-upstream-GPLv3`。本工具同样以 GPL-3.0 分发。

依赖的开源组件：WCDB.dll (BSD-3)、SDL2 (zlib)、wx_key.dll (MIT)、Electron (MIT)、
koffi (MIT)、fzstd (MIT)、PyInstaller (GPL-2.0)、ffmpeg (GPL)、fpdf2 (LGPL)、openpyxl (MIT)。

**仅供导出本人的聊天记录备份使用，请遵守相关法律法规，不要用于侵犯他人隐私。**
